---
# Konfigurasyon dosyalarinin listesini iceren dosyadir
pwlm_conf_list:
  - "{{ nginx['conf']['destination'] }}"
  - "{{ nginx['certificate']['destination'] }}"
  - "{{ nginx['key']['destination'] }}"
  - "{{ pwlm['conf']['destination'] }}"
  - "{{ uwsgi_services['service01']['destination'] }}"