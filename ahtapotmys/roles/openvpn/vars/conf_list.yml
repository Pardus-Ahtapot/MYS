---
# Konfigurasyon dosyalarinin listesini iceren dosyadir
openvpn_conf_list:
  - "{{ openvpn['default']['destination'] }}"
  - "{{ openvpn['cacrt']['destination'] }}"
  - "/etc/openvpn/dh2048.pem"
  - "{{ openvpn['cert']['destination'] }}"
  - "{{ openvpn['key']['destination'] }}"
  - "{{ openvpn['conf']['destination'] }}"
  - "{{ openvpn['ldapconf']['destination'] }}"