---
# Konfigurasyon dosyalarinin listesini iceren dosyadir
rsyslog_conf_list:
  - "{{ rsyslog['conf']['destination'] }}"
  - "{{ logrotate['conf']['destination'] }}"
  - "{{ signer['conf']['destination'] }}"
  - "{{ signer['cron']['destination'] }}"