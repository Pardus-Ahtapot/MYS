---
- name: apt bellegini guncelle
  apt: update_cache=yes cache_valid_time=3600

- name: Apachenin kuruldugunu onayla
  apt: "name={{ item }} state={{ apache_packages_state }}"
  with_items: "{{ apache_packages }}"
