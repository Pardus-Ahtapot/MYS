---
apache_enablerepo: ""

apache_listen_ip: "*"
apache_listen_port: 80
apache_listen_port_ssl: 443

apache_create_vhosts: true
apache_vhosts_filename: "vhosts.conf"
apache_vhosts_template: "vhosts.conf.j2"

# Debian/Pardus sistemlerde, varsayilan bir virtualhost konfigurasyonu paket icinde gelmektedir
# apache_remove_default_vhost dosyasini kaldirmak icin  `true` olarak ayarlayin
apache_remove_default_vhost: false

apache_global_vhost_settings: |
  DirectoryIndex index.php index.html

apache_vhosts:
  # Additional properties:
  # 'serveradmin, serveralias, allow_override, options, extra_parameters'.
  - servername: "local.dev"
    documentroot: "/var/www/html"

apache_allow_override: "All"
apache_options: "-Indexes +FollowSymLinks"

apache_vhosts_ssl: []
  # Ek parametreler:
  # 'serveradmin, serveralias, allow_override, options, extra_parameters'.
  # - servername: "local.dev",
  #   documentroot: "/var/www/html",
  #   certificate_file: "/path/to/certificate.crt",
  #   certificate_key_file: "/path/to/certificate.key",
  #   # Opsiyonel olarak:
  #   certificate_chain_file: "/path/to/certificate_chain.crt"

apache_ignore_missing_ssl_certificate: true

apache_ssl_protocol: "All -SSLv2 -SSLv3"
apache_ssl_cipher_suite: "AES256+EECDH:AES256+EDH"

# Sadece Debian/pardus sistemlerde
apache_mods_enabled:
  - rewrite.load
  - ssl.load
apache_mods_disabled: []

# Varsayilan apache durumu icin. Tavsiye edilen degerler: `started` yada `stopped`
apache_state: started

# Apache durumunu ayarla 
# `restarted` yada `reloaded`
apache_restart_state: restarted

# Apache paket durumu. kullanilabilir degerler:  `present` kurulmasis icin, yada `latest`
# ile guncel versiyona guncellemek icin
apache_packages_state: present
